# Base URL of the NestJS backend, including the /api prefix.
VITE_API_URL=http://localhost:3000/api

# Base URL of the Socket.io server (no path suffix — the /notifications
# namespace is appended by the client). Usually the same host/port as the
# API, without the /api prefix.
VITE_WS_URL=http://localhost:3000
